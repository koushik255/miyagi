# Miyagi

## Install

```sh
npm install
cd back
bun install
```

## Run

Start the local API:

```sh
cd back
bun run dev
```

Start the Electron app against the local API:

```sh
npm run dev
```

Or run the web app only:

```sh
npm run dev:web
```

## Check

```sh
npm run build
npm run lint
```
